# 智慧教学交互白板系统说明书

## 软件著作权申请相关说明
- **软件名称**：智慧教学交互白板系统（Aboard）
- **著作权人**：lifeafter619
- **开发与维护方式**：基于 Git 进行版本管理，代码按模块化方式组织于 `js/`、`css/` 等目录，所有变更通过提交记录留痕。
- **权属与许可**：项目版权声明见仓库根目录 `COPYRIGHT`，开源许可见 `LICENSE`（MIT）。

## 1. 系统简介
智慧教学交互白板系统是一款面向课堂讲解、板书演示和互动教学的网页白板工具。系统支持鼠标与触控操作，包含绘图、背景模板、多页管理、导入导出、教学小工具、计时与课堂互动功能。

## 2. 启动方式
### 2.1 本地启动
1. 进入项目根目录。
2. 执行：`npm start`
3. 浏览器访问：`http://localhost:8080`

> 注意：不要直接双击 `index.html` 打开，否则部分资源可能因浏览器安全策略无法加载。

### 2.2 部署启动
可部署到 GitHub Pages、Vercel、Cloudflare Pages 等静态托管平台，部署后直接访问地址即可使用。

## 3. 主界面说明
- **底部工具栏**：核心绘图与编辑操作入口。
- **右上角控制区**：导出、缩放、全屏、分页与时间显示。
- **画布区域**：进行板书、标注、图形绘制、文本与图片插入。
- **设置面板**：个性化配置、显示控制、画布模式与系统信息。

## 4. 功能操作指南

### 4.1 基础绘图
1. 点击工具栏中的“笔”。
2. 在画布按住并拖动即可书写。
3. 在笔属性中可调整：笔触类型、颜色、粗细。
4. 使用取色器可吸取任意颜色。

### 4.2 橡皮擦
1. 点击“橡皮”。
2. 选择橡皮形状（圆形/方形）和大小。
3. 在画布拖动擦除笔迹。

### 4.3 撤销与重做
- 点击工具栏或右上角按钮执行撤销/重做。
- 快捷键：`Ctrl+Z`（撤销）、`Ctrl+Y`（重做）。

### 4.4 画布移动与缩放
- 点击“移动”后拖动画布。
- 鼠标滚轮或右上角 `+/-` 按钮可缩放。
- 可直接输入缩放比例进行精确调整。

### 4.5 背景设置
1. 点击“背景”按钮。
2. 可设置背景颜色、图案类型（点阵、方格、田字格、坐标系等）。
3. 可调图案密度与透明度。
4. 支持上传图片作为背景并调节位置、缩放、旋转、翻转。

### 4.6 多页管理（分页模式）
1. 在设置中启用分页模式。
2. 使用右上角分页控件切换页面。
3. 可新增页面并在不同页面独立书写内容。

### 4.7 形状绘制
1. 选择形状工具（如直线、矩形、圆形等）。
2. 在画布拖动生成图形。
3. 可设置线宽、颜色和样式。

### 4.8 文本插入
1. 选择“插入文字”功能。
2. 点击画布后输入文本。
3. 可调整字号、字体、颜色、加粗、斜体、下划线、删除线。
4. 支持移动、缩放、旋转文本对象。

### 4.9 图片插入
1. 点击“插入图片”。
2. 选择本地图片文件上传。
3. 通过控制点调整位置、大小、旋转。
4. 支持水平/垂直翻转。

### 4.10 教学工具（直尺/三角板）
1. 打开“教具”功能并选择工具。
2. 拖动教具到目标位置。
3. 调整教具角度和大小。
4. 配合边缘绘制能力完成几何作图。

### 4.11 导入与导出
#### 导出
1. 点击右上角导出按钮。
2. 可导出为 PNG/JPEG 图片，支持当前页、全部页或指定页导出。
3. 可导出项目文件（`.aboard`），用于后续继续编辑。

#### 导入
1. 点击“导入项目”。
2. 选择 `.aboard` 或兼容数据文件。
3. 导入后恢复对应页面内容与设置。

### 4.12 课堂辅助功能（更多）
在“更多”中可使用：
- **时间显示**：展示当前日期与时间，可配置显示格式。
- **计时器**：课堂倒计时、计时提醒。
- **随机点名器**：用于课堂互动抽选。
- **计分板**：用于小组竞赛计分。

### 4.13 全屏模式
- 点击右上角全屏按钮进入沉浸模式，再次点击退出。

### 4.14 设置中心
设置面板支持：
- 通用设置（语言、交互偏好等）
- 显示设置（界面显示项开关）
- 画布设置（无限画布/分页、尺寸等）
- 背景设置（默认背景样式与参数）
- 公告与关于信息查看

## 5. 数据保存与恢复
- 系统使用本地数据库自动保存当前会话。
- 刷新页面或重新打开时，系统可提示恢复上次内容。
- 建议重要内容同时导出项目文件进行备份。

## 6. 快捷操作建议
- 使用触控大屏时，建议搭配“移动 + 缩放 + 教具”完成板书演示。
- 课堂前可先设置统一背景模板，减少上课切换时间。
- 课后使用导出图片或项目文件进行资料沉淀。

## 7. 常见问题
1. **页面打开空白或功能异常**：请确认通过本地服务器或已部署地址访问。
2. **导入失败**：请确认文件格式为系统支持的项目文件。
3. **误清空内容**：优先尝试撤销恢复；重要内容建议提前导出备份。

## 8. 适用场景
- 课堂板书与讲题
- 线上讲解与屏幕共享演示
- 教研备课与互动活动
- 小组竞赛计分与课堂组织
