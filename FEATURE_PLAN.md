# Aboard 功能演进计划

> 基于与 Excalidraw / Miro / FigJam / tldraw 等成熟白板产品的对比分析

## 现有功能盘点

| 类别 | Aboard 已有 |
|---|---|
| **绘图工具** | 普通笔、铅笔、圆珠笔、钢笔、毛笔、马克笔；线条样式（实线/虚线/多线）；橡皮擦（圆形/方形） |
| **形状** | 直线、箭头、双箭头、矩形、圆、椭圆 |
| **文本** | 插入文本、富文本解析 |
| **图片** | 插入图片/贴图、GIF 支持 |
| **选择** | 点击选择、框选、套索选择；拖拽/旋转/缩放；复制/粘贴/删除；编组/取消编组 |
| **背景** | 多种背景（空白/点阵/方格/田字格/四线格/五线谱/坐标系/极坐标/自定义图片）；颜色/透明度/密度调节 |
| **画布** | 无限画布 + 分页模式（A4/A3/B5）；缩放/平移；全屏 |
| **历史** | 撤销/重做（最多 50 步，512MB 内存上限） |
| **导入导出** | 导出图片（PNG/JPEG）；导出项目（.zip）；导入项目 |
| **教学工具** | 直尺（两种样式）；教学工具面板 |
| **辅助功能** | 计时器、时间显示、随机抽签、计分板 |
| **国际化** | 8 种语言（中简/中繁/英/日/韩/法/德/西） |
| **PWA** | 离线支持、Service Worker 更新机制 |
| **设置** | 主题色自定义、画布/显示/背景设置 |
| **快捷键** | Ctrl+Z/Y 撤销重做、Ctrl+C/V/X 复制粘贴、+/- 缩放、Delete 删除 |

---

## 第一阶段：核心体验提升（高优先级）

> 预计周期：1-2 个月
> 目标：从"画板工具"进化为"白板工具"

### 1. 便签（Sticky Notes）

**参考产品**：Miro, FigJam, Excalidraw

**功能描述**：
- 可拖拽的彩色便签卡片，支持文字编辑、颜色切换、缩放
- 白板最核心的元素之一，是结构化信息组织的基础

**技术要点**：
- 新增 `StickyNote` 对象类型，存储到 `drawingEngine.strokes` 或独立数组
- 便签数据结构：`{ type: 'sticky', x, y, width, height, text, color, fontSize, layerOrder }`
- 预设 6-8 种便签颜色（黄/粉/蓝/绿/紫/橙）
- 双击进入编辑模式，使用 contentEditable div 覆盖在 canvas 上方
- 复用现有的选择/拖拽/缩放/旋转逻辑（SelectionManager）
- 渲染到 canvas 上（renderScene 中添加 sticky 渲染分支）

### 2. 连接线/箭头连接器（Connectors）

**参考产品**：Miro, FigJam, tldraw

**功能描述**：
- 对象之间的自动连接线，拖动对象时连接线自动跟随
- 支持直线/折线/曲线三种连接模式
- 这是从"画板"进化到"白板"的关键功能

**技术要点**：
- 新增 `Connector` 对象类型：`{ type: 'connector', startId, endId, startAnchor, endAnchor, path, style }`
- 每个可连接对象需要锚点（上/下/左/右/中心），鼠标悬停时高亮显示
- 路由算法：直线模式直接连接；折线模式使用 A* 或正交路由避开其他对象；曲线模式使用贝塞尔曲线
- 拖动对象时，遍历所有连接线更新路径（性能关键：维护 `objectId -> connectors[]` 的映射）
- 连接线也支持选择/删除/样式修改（颜色/粗细/箭头类型）

### 3. 更多形状 + 形状内文字

**参考产品**：Excalidraw, tldraw

**功能描述**：
- 新增形状：三角形、菱形、平行四边形、星形、对话气泡、圆角矩形
- 所有形状支持双击输入文字（文字自动居中、自动换行）

**技术要点**：
- 扩展 `shape-drawing.js` 的形状绘制函数
- 形状数据结构增加 `text` 字段
- 文字渲染：`ctx.fillText` + 手动换行计算
- 双击形状时弹出 contentEditable 编辑框（复用便签的编辑逻辑）

### 4. 暗色模式（Dark Mode）

**参考产品**：Excalidraw, tldraw

**功能描述**：
- 完整的暗色主题，不仅是背景色切换，而是整个 UI（工具栏/面板/弹窗）的暗色适配
- 支持跟随系统主题自动切换

**技术要点**：
- CSS 变量体系：所有颜色使用 CSS 自定义属性，暗色模式只需切换变量值
- `<html data-theme="dark">` 切换，CSS 中 `[data-theme="dark"]` 选择器覆盖
- 设置中增加三个选项：浅色 / 暗色 / 跟随系统（`prefers-color-scheme`）
- Canvas 内容不受影响（用户绘制的内容颜色不变）
- 注意：导出图片时需要判断是否使用暗色背景

### 5. 对齐辅助线（Smart Guides）

**参考产品**：Figma, FigJam, Miro

**功能描述**：
- 拖动对象时自动显示对齐参考线（水平/垂直/中心对齐）
- 吸附到其他对象边缘，显示距离标注

**技术要点**：
- 拖动时遍历所有其他对象的边界（top/bottom/left/right/centerX/centerY）
- 对比当前拖动对象的对应边，阈值内（如 5px）触发吸附
- 绘制辅助线：在独立的 overlay canvas 或 SVG 层上绘制
- 性能优化：对象较多时使用空间索引（如四叉树）减少遍历

### 6. 手绘风格（Sketchy Style）

**参考产品**：Excalidraw

**功能描述**：
- 类似 Excalidraw 的手绘/素描风格渲染
- 让形状和线条看起来像手画的，可作为可选风格切换

**技术要点**：
- 使用 [roughjs](https://roughjs.com/) 库渲染手绘风格形状
- 设置中增加风格切换：精确 / 手绘
- 手绘模式下，形状绘制改用 roughjs 的 `rectangle()`、`circle()`、`line()` 等 API
- roughjs 约 30KB gzip，可懒加载

---

## 第二阶段：效率与表达力（中优先级）

> 预计周期：2-3 个月
> 目标：提升操作效率和信息表达能力

### 7. 模板库

**参考产品**：Miro, FigJam

**功能描述**：
- 预设模板：思维导图、流程图、看板（Kanban）、SWOT 分析、时间线、周计划等
- 一键创建常用布局，降低使用门槛

**技术要点**：
- 模板数据以 JSON 存储（与项目导出格式兼容）
- 新增模板选择弹窗，按分类展示缩略图
- 支持用户将当前画布保存为自定义模板

### 8. 快捷键增强

**参考产品**：Excalidraw, tldraw

**功能描述**：
- 单键工具切换：1=选择, 2=画笔, 3=橡皮, 4=形状, 5=文本, 6=图片, H=平移
- 形状快捷键：R=矩形, O=椭圆, L=直线, A=箭头
- 显示快捷键提示面板（? 键打开）
- Ctrl+D 复制并偏移、Ctrl+G 编组、Ctrl+Shift+G 取消编组

**技术要点**：
- 扩展 `board-helpers-runtime.js` 的 `setupKeyboardShortcuts()`
- 快捷键配置可自定义（存储到 localStorage）
- 帮助面板展示所有可用快捷键

### 9. 图层面板

**参考产品**：Figma

**功能描述**：
- 可视化的图层列表面板，显示所有对象
- 支持拖拽排序、锁定、显示/隐藏、重命名
- 当前只有内部 `layerOrder` 排序，用户无法直观管理

**技术要点**：
- 侧边面板 UI，列出所有对象（笔迹/形状/文本/图片/便签/编组）
- 每个图层项显示类型图标 + 缩略预览 + 名称
- 拖拽排序更新 `layerOrder`
- 锁定/隐藏状态存储在对象数据中

### 10. 对象锁定

**参考产品**：Miro, Figma

**功能描述**：
- 锁定对象防止误操作（移动/编辑/删除）
- 锁定后显示锁定图标，右键菜单可解锁

**技术要点**：
- 对象数据增加 `locked: boolean` 字段
- `SelectionManager` 的 hit test 跳过锁定对象
- 锁定对象渲染时显示锁定指示器（小锁图标）

### 11. 填充色

**参考产品**：Excalidraw, tldraw

**功能描述**：
- 形状支持填充颜色（纯色/半透明）
- 当前形状只有描边，没有填充能力

**技术要点**：
- 形状数据增加 `fillColor` 和 `fillOpacity` 字段
- 形状配置面板增加填充色选择器
- `ctx.fill()` 在 `ctx.stroke()` 之前调用
- 支持"无填充"选项

### 12. 激光笔 / 演示模式

**参考产品**：Miro, FigJam

**功能描述**：
- **激光笔**：红色圆点跟随鼠标，留下短暂轨迹后渐隐消失
- **演示模式**：隐藏所有 UI（工具栏/面板/控件），只显示画布内容，按 Esc 退出

**技术要点**：
- 激光笔：独立 canvas 层，存储最近 N 个点 + 时间戳，每帧渲染时根据年龄设置透明度
- 演示模式：CSS 隐藏所有 UI 元素，进入类全屏状态，只保留 canvas 和缩放功能

### 13. 框架 / 画板（Frame）

**参考产品**：Figma, FigJam, Miro

**功能描述**：
- 可命名的矩形区域，用于组织内容分区
- 移动 Frame 时，其内部的所有对象跟随移动
- 导出时可按 Frame 单独导出

**技术要点**：
- `Frame` 对象类型：`{ type: 'frame', x, y, width, height, name, children: [] }`
- 包含关系检测：对象中心在 Frame 内则认为属于该 Frame
- 渲染：带标题的虚线矩形边框，标题在左上角

---

## 第三阶段：协作与高级功能（长期目标）

> 预计周期：持续迭代
> 目标：建立协作生态和智能能力

### 14. 实时协作

**参考产品**：Miro, FigJam, Excalidraw+

**功能描述**：
- WebSocket 多人实时编辑
- 每个用户显示彩色光标和用户名
- 同时编辑不同区域不冲突

**技术要点**：
- 需要后端服务（Node.js + WebSocket / Socket.io）
- 操作同步方案：CRDT（如 Yjs / Automerge）或 OT（Operational Transform）
- 推荐使用 Yjs + y-websocket，成熟度最高
- 对象级粒度的冲突解决（非像素级）
- 需要用户身份系统（至少支持匿名 + 昵称）

### 15. 评论 / 批注

**参考产品**：Miro, FigJam

**功能描述**：
- 在画布任意位置添加评论气泡
- 支持回复线程
- 评论可标记为已解决

### 16. 思维导图模式

**参考产品**：Miro, XMind

**功能描述**：
- 专用的思维导图编辑模式
- 自动布局（树形/放射状）
- Tab 键添加子节点，Enter 添加兄弟节点
- 节点自动用连接线关联

### 17. 流程图模式

**参考产品**：Miro, draw.io

**功能描述**：
- 专用流程图编辑
- 标准节点类型（开始/结束/判断/处理/输入输出）
- 自动布局和对齐
- 连接点吸附

### 18. 插件 / 扩展系统

**参考产品**：Miro, Figma

**功能描述**：
- 开放 API 让社区开发插件
- 自定义形状、数据源集成、第三方服务对接

### 19. 云端存储与同步

**参考产品**：Miro, FigJam

**功能描述**：
- 用户账户系统
- 项目云端保存
- 多设备同步
- 分享链接（只读/可编辑）

### 20. AI 辅助

**参考产品**：FigJam AI, Miro AI

**功能描述**：
- AI 生成图表（描述需求自动生成流程图/思维导图）
- AI 整理便签（自动分类/聚合相似内容）
- AI 总结白板内容
- 手写识别转文字

---

## 建议实施路线

```
第一阶段（1-2个月）—— 核心体验
├── 便签 Sticky Notes ← 最高优先级，白板核心元素
├── 连接线 Connectors ← 从画板到白板的质变
├── 更多形状 + 形状内文字
├── 暗色模式 Dark Mode
├── 对齐辅助线 Smart Guides
└── 手绘风格 Sketchy Style（可选）

第二阶段（2-3个月）—— 效率提升
├── 快捷键增强
├── 图层面板
├── 对象锁定
├── 形状填充色
├── 激光笔 / 演示模式
├── 框架 Frame
└── 模板库

第三阶段（持续迭代）—— 协作生态
├── 实时协作（WebSocket + CRDT）
├── 评论批注
├── 思维导图 / 流程图模式
├── 云端存储
└── AI 辅助 / 插件系统
```

> **核心建议**：第一阶段的 **便签** 和 **连接线** 是最值得优先做的两个功能。
> 它们是区分"画板工具"和"白板工具"的关键分水岭。
> Aboard 目前的绘图能力很强（6 种笔型、丰富的背景模式），但缺少结构化的信息组织能力。
> 便签 + 连接线能让用户从"画画"升级到"思考"。
